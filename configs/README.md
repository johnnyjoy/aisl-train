# configs/

Experiment JSON consumed by `aisl-train train --config`.

`pilot-qlora.json` is a conservative first QLoRA setup, not a
tuned hyperparameter result. Dataset and model paths stay on the
command line.
