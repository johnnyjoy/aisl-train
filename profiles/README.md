# profiles/

JSON overrides for architecture-specific exceptions. See
[docs/model-profiles.md](../docs/model-profiles.md).

`qwen3.8-27b.json` is minimal and pending runtime validation
against a real checkpoint.
